const validator = {}
 





// crear una funcion isvali
/*function isVali (finalizarBoton)
let newfinalizarBoton = (Array.from(finalizarBoton));
 let reverseTarg = newfinalizarBoton, reverse().map(x=> parseInt(x));
 let resul =0;
 for (let i = 0; i % 2 ===0){
  if ( i===0 || i % 2 ===0){
     let noMultiply = (reverseTarg[i]);
     resul +=noMultiply;
    } else {
      let multiply = (reverseTarg[i] * 2);
      if (multiply >= 10) {
        result += multiply - 9;
      }
    }
 }

*/


export default validator;


